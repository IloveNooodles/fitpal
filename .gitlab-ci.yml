image: python:latest

stages:
    - linting

isort:
    stage: linting
    tags: 
        - ci/cd-linting
    script:
        - isort . --check-only
    only:
        - merge_request

black:
    stage: linting
    tags: 
        - ci/cd-linting
    script:
        - black --check .
    only:
        - merge_request

flake8:
    stage: linting
    tags: 
        - ci/cd-linting
    script:
        - flake8 .
    only:
        - merge_request

pylint:
    stage: linting
    tags: 
        - ci/cd-linting
    script:
        - pylint $(find . -iname "*.py")
    only:
        - merge_request