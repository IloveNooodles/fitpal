image: python:latest

stages:
    - linting

isort:
    stage: linting
    tags: 
        - pdss-dev
    script:
        - isort . --check-only
    only:
        - merge_request

black:
    stage: linting
    tags: 
        - pdss-dev
    script:
        - black --check .
    only:
        - merge_request

flake8:
    stage: linting
    tags: 
        - pdss-dev
    script:
        - flake8 .
    only:
        - merge_request

pylint:
    stage: linting
    tags: 
        - pdss-dev
    script:
        - pylint $(find . -iname "*.py")
    only:
        - merge_request